name: Build Android Widget APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'android-widget/**'
      - '.github/workflows/build-android-widget.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android 35
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build debug APK
        run: gradle -p android-widget :app:assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Projets-Electriques-Widget-Android
          path: android-widget/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
